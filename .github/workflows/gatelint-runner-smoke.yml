name: gatelint-runner-smoke

on:
  push:
    branches:
      - work/gatelint-real-proof-20260826
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          set -euo pipefail
          uname -a
          echo GATELINT_RUNNER_OK
