name: gatelint-runner-smoke

on:
  pull_request:
    paths:
      - '.github/workflows/gatelint-runner-smoke.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          set -euo pipefail
          uname -a
          echo runner-ok
